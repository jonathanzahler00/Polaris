# Polaris Android Widget – single source of truth for version numbers.
# Bump VERSION_CODE for every upload (Play Store / GitHub / sideload). Must increase each time.
# VERSION_NAME: semantic versioning. Beta: use -beta.N suffix (e.g. 1.9.0-beta.1). Release: plain (e.g. 1.9.0).
#
# Build variants (see VERSIONING.md):
#   ./gradlew assembleProdRelease   → production (com.polaris.widget)
#   ./gradlew assembleBetaRelease   → beta side-by-side install (com.polaris.widget.beta, name ends with -beta)

VERSION_CODE=14
VERSION_NAME=1.9.0
